import { DateCountPipePipe } from './date-count-pipe.pipe';

describe('DateCountPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DateCountPipePipe();
    expect(pipe).toBeTruthy();
  });
});
